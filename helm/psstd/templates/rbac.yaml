apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "psstd.fullname" . }}
---
# psstd doesn't need k8s API access — gossip handles discovery.
# Add Node read rules here if you later want k8s-native node metadata.
